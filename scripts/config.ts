/**
 * plugin.json 自動生成スクリプトの設定
 */

/**
 * 自動生成の対象から除外するカテゴリ
 *
 * 以下の場合は除外に追加してください:
 * - サブモジュール（外部リポジトリで管理）
 * - 構造が異なり自動生成に対応できないもの
 * - 手動管理が必要なもの
 */
export const EXCLUDED_CATEGORIES: string[] = [
  'openai-skills', // サブモジュール: OpenAI公式スキル（上流で管理）
];
